#pragma once

/* TODO: The gameplayframework header grants users access to all gameplayframework concerning headers. This includes:
 * GameObject as an object that can belong to a Level and can tick components
 * Components as logic that that GameObjects can tick
 * IProxyObject Template interface that helps create proxy GameObjects for "Parrallel worlds" (e.g. RenderScene and PhysicsWorld)
 * LevelObjects are a type of GameObject that can be placed in a Level (i.e. has a transform component)
 * PrimitiveObjects are LevelObjects that can be rendered (i.e., Meshes or sprites)
 * Gamemodes as global World bound state holders
 */
