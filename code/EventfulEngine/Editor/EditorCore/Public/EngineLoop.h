#pragma once

#include "IEngineLoop.h"

namespace EventfulEditor{
    /*TODO: Editor IEngineLoop that runs as an inner loop
     *Initializes editor level modules
     *Creates window using UI Module
     *Constructs a Dock-Tab based editor using the UI module*/
    class EngineLoop : public EventfulEngine::IEngineLoop{

    };
}
